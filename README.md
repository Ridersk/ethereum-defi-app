# Ethereum Defi App

## Setup

Install Truffle:

```shell
npm install -g global truffle
```

Check version:

```shell
truffle version
```

Compile:

```shell
truffle compile
```

Deploy

```shell
truffle migrate
```
